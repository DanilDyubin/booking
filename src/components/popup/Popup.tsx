import styles from './styles.module.scss';

export const Popup = () => {
  return (
    <div className={styles.popup}>
      <ul>
        <li></li>
      </ul>
    </div>
  );
};

export default Popup;
